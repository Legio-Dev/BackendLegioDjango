from django.apps import AppConfig

class VisitasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.visitas' # <--- IMPORTANTE: Debe incluir el prefijo 'apps.'